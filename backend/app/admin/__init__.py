"""Admin context — internal-only reporting endpoints.

Lives under `/v1/admin/*` and is gated by the `admin` user role.
Phase 5 currently exposes the AI spend report; future sessions add
moderation queue + analytics dashboards here.
"""
